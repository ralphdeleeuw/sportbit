name: Fetch Runna

on:
  schedule:
    # Dagelijks om 07:30 Amsterdam tijd (05:30 UTC zomertijd)
    - cron: '30 5 * * *'
  workflow_dispatch:

jobs:
  fetch-runna:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests playwright

      - name: Install Playwright browsers
        run: playwright install chromium --with-deps

      - name: Fetch Runna data and save to Gist
        env:
          RUNNA_EMAIL: ${{ secrets.RUNNA_EMAIL }}
          RUNNA_PASSWORD: ${{ secrets.RUNNA_PASSWORD }}
          GIST_ID: ${{ secrets.GIST_ID }}
          GITHUB_TOKEN: ${{ secrets.GIST_TOKEN }}
          # Optioneel: mobile API-key uit APK-analyse (zie find_runna_mobile_key.py)
          # Voeg toe als GitHub Secret RUNNA_MOBILE_API_KEY om sessiedata op te halen
          RUNNA_MOBILE_API_KEY: ${{ secrets.RUNNA_MOBILE_API_KEY }}
        run: python3 fetch_runna.py
